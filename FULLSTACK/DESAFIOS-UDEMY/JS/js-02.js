var names = ["Angela", "Ben", "Jenny", "Michael", "Chloe"];


function whosPaying(names) {
    var n = names.length - 1
    var aleatorio = Math.random()
    n = (n * aleatorio) 
    var aleatorion = Math.round(n)
    console.log(names[aleatorion] + " is going to buy lunch today")
}